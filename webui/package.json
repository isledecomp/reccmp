{
  "name": "reccmp-js-tests",
  "version": "1.0.0",
  "description": "This is only to run tests/linting, not for building.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "e2e": "playwright test",
    "e2e-ui": "playwright test --ui",
    "lint": "biome check e2e ./testdata.json ../reccmp/assets/*.js ../reccmp/assets/components/*.js"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.0.8"
  }
}
