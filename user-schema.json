{
  "$defs": {
    "UserFileTarget": {
      "description": "Target schema for user.yml",
      "properties": {
        "path": {
          "description": "Path to the original executable file",
          "format": "path",
          "title": "Path",
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "title": "UserFileTarget",
      "type": "object"
    }
  },
  "description": "File schema for user.yml",
  "properties": {
    "targets": {
      "additionalProperties": {
        "$ref": "#/$defs/UserFileTarget"
      },
      "description": "List of targets",
      "title": "Targets",
      "type": "object"
    }
  },
  "required": [
    "targets"
  ],
  "title": "UserFile",
  "type": "object"
}